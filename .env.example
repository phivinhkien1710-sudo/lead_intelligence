# Optional fallback enrichment providers.
# The pipeline uses public datasets, website discovery, website scraping, and
# rule-based extraction first. These keys are only used when important fields
# are still missing or uncertain.

TAVILY_API_KEY=
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional polite crawler identity.
LEAD_INTELLIGENCE_USER_AGENT=LeadIntelligencePipeline/1.0 (+contact@example.com)

